data_loading:
  dataset_name: "tiles-8k"
  batch_size: 8
  val_split: 0.2
  preload: true
  normalize: true

training:
  num_epochs: 20
  learning_rate: 0.0005
  device: "auto"
  output_dir: "outputs/training_results"
